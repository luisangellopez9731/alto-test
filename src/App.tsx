import { Router } from './components'
import styled from "styled-components"

const AppWrapper = styled.div`
`

function App() {

  return (
    <AppWrapper>
      <Router />
    </AppWrapper>
  )
}

export default App
